.header(ng-controller='NavbarCtrl')
  nav
    ul.nav
      li(class='{{item.class}}', ng-repeat='item in menu', ng-class='{active: isActive(item.link)}')
        a(ng-click='gotoAnchor(item.link)') {{item.title}}
    p.date October 18, 2014
 
