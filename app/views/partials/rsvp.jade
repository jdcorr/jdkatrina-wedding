#rsvp(ng-controller='RsvpCtrl').container-full
  h2 RSVP
  .dark-container  
    .rsvp-form
      ng-switch(on="state")
        form.rsvp-code-form(ng-switch-when="step1") 
          p Enter the rsvp code on your invitation. If you are unable to locate the code or have misplaced it, please 
            a(href='mailto:rsvp@jdandkatrina.com') contact us
            |. 
          input.rsvp-code-input(ng-model="rsvp.code", placeholder="Enter Code Here", max-length=5, required, ng-minlength="4", ng-maxlength="6", ng-change="checkForRsvpCode()", name="rsvp-input")
          button.continue-btn(ng-click="getRsvp()")
            span Continue
        form.rsvp-info-form(ng-switch-when="step2")
          p Review the information below and update as needed, then click submit. Please 
            a(href='mailto:rsvp@jdandkatrina.com') contact us 
            | with any questions.
          .form-line
            label First Name:
            input.first-name(type="text", placeholder="First Name", ng-model="rsvp.firstName")
          .form-line
            label Last Name:
            input.last-name(type="text", placeholder="Last Name", ng-model="rsvp.lastName")
          .form-line
            label Email: 
            input.email(type="email", placeholder="Email (optional)", ng-model="rsvp.email") 
          .form-line(ng-if="rsvp.isAllowedGuests")
            label Total Adults:
            input.total-adults(type="number", placeholder="total number of adults (including you)", ng-model="rsvp.totalAdults", max=4, min=1, ng-show="rsvp.isAllowedGuests")
          .form-line(ng-if="rsvp.totalChildren")
            label Total Children (10 and under):
              input.total-children(type="number", placeholder="total number of adults (including you)", ng-model="rsvp.totalChildren", max=4, min=1, ng-show="rsvp.isAllowedGuests")
          .form-line(ng-if="rsvp.totalInfants")  
            label Total Infants (3 and under):  
              input.total-infants(type="number", placeholder="total number of adults (including you)", ng-model="rsvp.totalInfants", max=4, min=1, ng-show="rsvp.isAllowedGuests")  
          button.continue-btn(ng-click="showPreSubmit()")
            span Continue
        form.rsvp-verify-form(ng-switch-when="step3")
          p Review the following info and click submit to complete your rsvp:
          p Name: 
            strong {{rsvp.firstName}} {{rsvp.lastName}}          
          p(ng-if="rsvp.email") Email: 
            strong {{rsvp.email}}
          p(ng-if="rsvp.isAllowedGuests") Total Adults: 
            strong {{rsvp.totalAdults}}
          p(ng-if="rsvp.totalChildren") Total Children: 
            strong {{rsvp.totalChildren}}
          p(ng-if="rsvp.totalInfants") Total Infants: 
            strong {{rsvp.totalInfants}}
          button.change-btn(ng-click="backToInfo()")
            span Change
          button.submit-btn(ng-click="showPreSubmit()")
            span Submit
        .completion(ng-switch-when="completion")
          p Thank you for your RSVP! We cannot wait to celebrate with you on the big day. 